<section class="brand-story section">
  <div class="container brand-story__grid">
    <div class="brand-story__index mono">01 / APPROACH</div>
    <div class="brand-story__statement">
      <p>我不把 AI 当作一个需要被放大的标签。</p>
      <h2>先理解人的任务，再决定模型、工作流与边界应该如何出现。</h2>
    </div>
    <div class="brand-story__note">
      <span class="eyebrow">FROM ENGINEERING TO AI</span>
      <p>
        交通工程训练让我习惯观察复杂系统中的参与者、规则与异常；产品实践让我把这种系统视角转化为用户路径、原型和评测闭环。
      </p>
    </div>
  </div>
</section>

<style>
  .brand-story {
    background: rgba(255, 255, 255, 0.46);
  }

  .brand-story__grid {
    display: grid;
    grid-template-columns: minmax(100px, 0.28fr) minmax(0, 1.35fr) minmax(240px, 0.48fr);
    gap: clamp(2rem, 5vw, 6rem);
    align-items: start;
  }

  .brand-story__index {
    color: var(--ink-500);
    font-size: 0.7rem;
  }

  .brand-story__statement p {
    color: var(--ink-500);
  }

  .brand-story__statement h2 {
    max-width: 900px;
    margin: 1.3rem 0 0;
    font-size: clamp(2.5rem, 5.2vw, 5.6rem);
    font-weight: 560;
    letter-spacing: -0.06em;
    line-height: 1.08;
  }

  .brand-story__note {
    padding-top: 35%;
  }

  .brand-story__note p {
    margin: 1.1rem 0 0;
    color: var(--ink-650);
    line-height: 1.85;
  }

  @media (max-width: 900px) {
    .brand-story__grid {
      grid-template-columns: 80px 1fr;
    }

    .brand-story__note {
      grid-column: 2;
      max-width: 650px;
      padding-top: 1rem;
    }
  }

  @media (max-width: 560px) {
    .brand-story__grid {
      grid-template-columns: 1fr;
    }

    .brand-story__note {
      grid-column: 1;
    }
  }
</style>
