---
interface Props {
  title: string;
  items: string[];
}

const { title, items } = Astro.props;
---

<section class="scope-column">
  <h3>{title}</h3>
  <ul>{items.map((item) => <li>{item}</li>)}</ul>
</section>
