<div class="main-grid page-shell">
  <aside class="main-grid__left" aria-label="个人资料"><slot name="left" /></aside>
  <section class="main-grid__content"><slot /></section>
  <aside class="main-grid__right" aria-label="站点信息"><slot name="right" /></aside>
</div>

<style>
  .main-grid {
    position: relative;
    z-index: 4;
    display: grid;
    grid-template-columns: minmax(214px, 0.66fr) minmax(0, 2.36fr) minmax(208px, 0.66fr);
    gap: 1.15rem;
    align-items: start;
    margin-top: -2.7rem;
    padding-bottom: 5.5rem;
  }

  .main-grid__left,
  .main-grid__right {
    position: sticky;
    top: 94px;
    display: grid;
    gap: 1.15rem;
  }

  .main-grid__content {
    display: grid;
    min-width: 0;
    gap: 1.15rem;
  }

  @media (max-width: 1180px) {
    .main-grid {
      grid-template-columns: minmax(210px, 0.64fr) minmax(0, 1.86fr);
    }

    .main-grid__right {
      position: static;
      grid-column: 1 / -1;
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 760px) {
    .main-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
      margin-top: -1.35rem;
      padding-bottom: 4rem;
    }

    .main-grid__left,
    .main-grid__right {
      position: static;
      grid-column: 1;
    }

    .main-grid__right {
      grid-template-columns: 1fr;
    }
  }
</style>
