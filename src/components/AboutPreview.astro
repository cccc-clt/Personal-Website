<section class="about-preview section">
  <div class="container about-preview__grid">
    <div class="about-preview__portrait" aria-hidden="true">
      <div class="about-preview__sun"></div>
      <span class="mono">TRAFFIC → PRODUCT → AI</span>
    </div>
    <div class="about-preview__copy">
      <span class="eyebrow">04 / ABOUT TIANQI</span>
      <h2 class="section-title">工程训练给我系统视角，产品实践让我持续靠近真实的人。</h2>
      <p class="lede">我关心的不只是模型能做什么，也关心用户何时需要解释、何时需要撤回、何时应该由人做最后决定。</p>
      <a class="text-link" href="/about">阅读我的跨专业路径</a>
    </div>
  </div>
</section>

<style>
  .about-preview {
    background: white;
  }

  .about-preview__grid {
    display: grid;
    grid-template-columns: minmax(320px, 0.7fr) minmax(0, 1.3fr);
    gap: clamp(3rem, 8vw, 9rem);
    align-items: center;
  }

  .about-preview__portrait {
    position: relative;
    min-height: 610px;
    overflow: hidden;
    border-radius: 48% 48% 32px 32px / 28% 28% 32px 32px;
    background:
      radial-gradient(circle at 50% 33%, #fff 0 8%, transparent 9%),
      radial-gradient(circle at 50% 38%, rgba(115, 157, 225, 0.55) 0 20%, transparent 21%),
      linear-gradient(160deg, #dff5ff, #deddfc 52%, #f4dfe4);
    box-shadow: var(--shadow-soft);
  }

  .about-preview__portrait::before {
    position: absolute;
    bottom: -14%;
    left: 50%;
    width: 78%;
    height: 55%;
    border-radius: 50% 50% 0 0;
    background: linear-gradient(150deg, rgba(255, 255, 255, 0.8), rgba(92, 114, 186, 0.85));
    content: '';
    translate: -50%;
  }

  .about-preview__portrait::after {
    position: absolute;
    inset: 15%;
    border: 1px solid rgba(255, 255, 255, 0.7);
    border-radius: 50%;
    content: '';
  }

  .about-preview__sun {
    position: absolute;
    top: 8%;
    right: 10%;
    width: 70px;
    height: 70px;
    border-radius: 50%;
    background: rgba(255, 225, 164, 0.7);
    filter: blur(1px);
  }

  .about-preview__portrait span {
    position: absolute;
    z-index: 2;
    bottom: 1.5rem;
    left: 1.5rem;
    color: rgba(255, 255, 255, 0.9);
    font-size: 0.66rem;
    letter-spacing: 0.12em;
  }

  .about-preview__copy h2 {
    margin: 1.5rem 0;
  }

  .about-preview__copy .text-link {
    margin-top: 1rem;
  }

  @media (max-width: 820px) {
    .about-preview__grid {
      grid-template-columns: 1fr;
    }

    .about-preview__portrait {
      min-height: 480px;
    }
  }
</style>

