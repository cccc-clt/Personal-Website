<div class="article-prose prose" data-article-content>
  <slot />
</div>

<style>
  .article-prose {
    min-width: 0;
    color: var(--text-secondary);
    font-size: clamp(1.03rem, 0.98rem + 0.2vw, 1.1rem);
    line-height: 1.85;
  }

  .article-prose :global(p) {
    margin: 0 0 1.4em;
    line-height: 1.85;
    text-wrap: pretty;
  }

  .article-prose :global(h2),
  .article-prose :global(h3) {
    color: var(--text-primary);
    scroll-margin-top: 7rem;
    text-wrap: balance;
  }

  .article-prose :global(h2) {
    margin: 3.6rem 0 1.25rem;
    padding-top: 0.2rem;
    font-size: clamp(1.85rem, 3.2vw, 2.55rem);
    letter-spacing: -0.02em;
    line-height: 1.28;
  }

  .article-prose :global(h2:first-child) {
    margin-top: 0;
  }

  .article-prose :global(h3) {
    margin: 2.5rem 0 0.9rem;
    font-size: clamp(1.25rem, 2vw, 1.55rem);
    letter-spacing: -0.01em;
    line-height: 1.4;
  }

  .article-prose :global(ul),
  .article-prose :global(ol) {
    display: grid;
    gap: 0.68rem;
    margin: 0 0 1.6rem;
    padding-left: 1.35rem;
  }

  .article-prose :global(li) {
    padding-left: 0.12rem;
    line-height: 1.82;
  }

  .article-prose :global(li::marker) {
    color: var(--accent);
  }

  .article-prose :global(blockquote) {
    margin: 2rem 0;
    padding: 1.1rem 1.2rem;
    border: 1px solid var(--border);
    border-left: 3px solid var(--accent);
    border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
    background: var(--surface-warm);
    color: var(--text-primary);
  }

  .article-prose :global(blockquote p:last-child) {
    margin-bottom: 0;
  }

  .article-prose :global(hr) {
    margin: 3.25rem 0;
    border: 0;
    border-top: 1px solid var(--border-strong);
  }

  .article-prose :global(figure) {
    margin: 2.5rem 0;
  }

  .article-prose :global(img) {
    display: block;
    max-width: 100%;
    height: auto;
    border-radius: var(--radius-sm);
  }

  .article-prose :global(figcaption) {
    margin-top: 0.75rem;
    color: var(--text-muted);
    font-size: 0.76rem;
    line-height: 1.65;
    text-align: center;
  }

  .article-prose :global(.article-experience) {
    display: grid;
    gap: 0.72rem;
    margin-top: 4rem;
    padding-block: 1.4rem;
    border-block: 1px solid var(--border-strong);
  }

  .article-prose :global(.article-experience p) {
    margin: 0;
  }

  .article-prose :global(.article-experience__label) {
    color: var(--text-muted);
    font-size: 0.74rem;
    font-weight: 650;
  }

  .article-prose :global(.article-experience__title) {
    color: var(--text-primary);
    font-family: var(--font-display);
    font-size: clamp(1.18rem, 2vw, 1.42rem);
    font-weight: 650;
    line-height: 1.5;
  }

  .article-prose :global(.article-experience__link) {
    display: inline-flex;
    width: fit-content;
    margin-top: 0.2rem;
    color: var(--accent-hover);
    font-size: 0.86rem;
    font-weight: 650;
    text-decoration: underline;
    text-decoration-color: color-mix(in srgb, currentColor 32%, transparent);
    text-underline-offset: 0.2em;
  }

  .article-prose :global(table) {
    display: block;
    width: 100%;
    max-width: 100%;
    margin: 1.8rem 0 2.2rem;
    overflow-x: auto;
    border-top: 1px solid var(--border-strong);
    border-bottom: 1px solid var(--border-strong);
    font-size: 0.88rem;
    line-height: 1.65;
    overscroll-behavior-inline: contain;
  }

  .article-prose :global(th),
  .article-prose :global(td) {
    min-width: 8.5rem;
    padding: 0.8rem 0.85rem;
    vertical-align: top;
    white-space: normal;
  }

  .article-prose :global(th) {
    background: var(--surface-soft);
    color: var(--text-primary);
    font-weight: 650;
  }

  .article-prose :global(code) {
    overflow-wrap: anywhere;
  }

  @media (max-width: 560px) {
    .article-prose {
      font-size: 1.0625rem;
    }

    .article-prose :global(h2) {
      margin-top: 3rem;
    }
  }
</style>
