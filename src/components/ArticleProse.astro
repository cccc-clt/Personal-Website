<div class="article-prose prose" data-article-content>
  <slot />
</div>

<style>
  .article-prose {
    min-width: 0;
    color: var(--text-secondary);
    font-size: clamp(1.03rem, 0.98rem + 0.2vw, 1.1rem);
    line-height: 1.85;
  }

  .article-prose :global(p) {
    margin: 0 0 1.4em;
    line-height: 1.85;
    text-wrap: pretty;
  }

  .article-prose :global(h2),
  .article-prose :global(h3),
  .article-prose :global(h4) {
    color: var(--text-primary);
    scroll-margin-top: 7rem;
    text-wrap: balance;
  }

  .article-prose :global(h2) {
    margin: 3.6rem 0 1.25rem;
    padding-top: 0.2rem;
    font-size: clamp(1.85rem, 3.2vw, 2.55rem);
    letter-spacing: -0.02em;
    line-height: 1.28;
  }

  .article-prose :global(h2:first-child) {
    margin-top: 0;
  }

  .article-prose :global(h3) {
    margin: 2.5rem 0 0.9rem;
    font-size: clamp(1.25rem, 2vw, 1.55rem);
    letter-spacing: -0.01em;
    line-height: 1.4;
  }

  .article-prose :global(h4) {
    margin: 2rem 0 0.75rem;
    font-size: clamp(1.08rem, 1.6vw, 1.22rem);
    letter-spacing: -0.006em;
    line-height: 1.5;
  }

  .article-prose :global(ul),
  .article-prose :global(ol) {
    display: grid;
    gap: 0.68rem;
    margin: 0 0 1.6rem;
    padding-left: 1.35rem;
  }

  .article-prose :global(li) {
    padding-left: 0.12rem;
    line-height: 1.82;
  }

  .article-prose :global(li::marker) {
    color: var(--accent);
  }

  .article-prose :global(blockquote) {
    margin: 2rem 0;
    padding: 1.1rem 1.2rem;
    border: 1px solid var(--border);
    border-left-color: var(--accent);
    border-radius: var(--radius-sm);
    background: var(--surface-warm);
    color: var(--text-primary);
  }

  .article-prose :global(blockquote p:last-child) {
    margin-bottom: 0;
  }

  .article-prose :global(.article-flow) {
    display: flex;
    flex-wrap: wrap;
    gap: 0.42rem 0.72rem;
    align-items: center;
    margin: 0.25rem 0 1.7rem;
    padding-block: 0.85rem;
    border-block: 1px solid var(--border);
    color: var(--text-primary);
    font-size: 0.92rem;
    font-weight: 620;
  }

  .article-prose :global(.article-flow span[aria-hidden='true']) {
    color: var(--accent);
    font-weight: 500;
  }

  .article-prose :global(.article-transform-list) {
    display: grid;
    margin: 1.7rem 0 2.1rem;
    border-block: 1px solid var(--border-strong);
  }

  .article-prose :global(.article-transform-list p) {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
    gap: 0.85rem;
    align-items: center;
    margin: 0;
    padding: 0.8rem 0;
    border-top: 1px solid var(--border);
    color: var(--text-secondary);
    font-size: 0.9rem;
  }

  .article-prose :global(.article-transform-list p:first-child) {
    border-top: 0;
  }

  .article-prose :global(.article-transform-list p > span[aria-hidden='true']) {
    color: var(--accent);
  }

  .article-prose :global(.article-transform-list strong) {
    color: var(--text-primary);
  }

  .article-prose :global(.article-capability-list) {
    display: grid;
    margin: 1.8rem 0 2.2rem;
    border-block: 1px solid var(--border-strong);
  }

  .article-prose :global(.article-capability-list > div) {
    display: grid;
    grid-template-columns: minmax(9rem, 0.32fr) minmax(0, 1fr);
    gap: 1.25rem;
    padding: 1rem 0;
    border-top: 1px solid var(--border);
  }

  .article-prose :global(.article-capability-list > div:first-child) {
    border-top: 0;
  }

  .article-prose :global(.article-capability-list dt),
  .article-prose :global(.article-capability-list dd) {
    margin: 0;
  }

  .article-prose :global(.article-capability-list dt) {
    color: var(--accent-hover);
    font-family: var(--font-mono);
    font-size: 0.82rem;
    font-weight: 680;
    overflow-wrap: anywhere;
  }

  .article-prose :global(.article-capability-list dd) {
    color: var(--text-secondary);
    font-size: 0.9rem;
    line-height: 1.75;
  }

  .article-prose :global(.article-capability-list dd strong) {
    display: block;
    margin-bottom: 0.15rem;
    color: var(--text-primary);
    font-size: 0.78rem;
  }

  .article-prose :global(.article-prompt-structure) {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0;
    margin: 1.8rem 0 2.2rem;
    padding: 0;
    border-block: 1px solid var(--border-strong);
    list-style: none;
  }

  .article-prose :global(.article-prompt-structure li) {
    display: grid;
    gap: 0.2rem;
    padding: 0.9rem 0.75rem;
    border-top: 1px solid var(--border);
    color: var(--text-primary);
    line-height: 1.4;
  }

  .article-prose :global(.article-prompt-structure li:nth-child(-n + 3)) {
    border-top: 0;
  }

  .article-prose :global(.article-prompt-structure li:not(:nth-child(3n + 1))) {
    border-left: 1px solid var(--border);
  }

  .article-prose :global(.article-prompt-structure strong) {
    font-family: var(--font-mono);
    font-size: 0.82rem;
    overflow-wrap: anywhere;
  }

  .article-prose :global(.article-prompt-structure span) {
    color: var(--text-muted);
    font-size: 0.72rem;
  }

  .article-prose :global(.article-vertical-flow) {
    position: relative;
    display: grid;
    gap: 0.55rem;
    margin: 1.8rem 0 2.2rem;
    padding: 0;
    list-style: none;
  }

  .article-prose :global(.article-vertical-flow::before) {
    position: absolute;
    top: 1.5rem;
    bottom: 1.5rem;
    left: 1rem;
    width: 1px;
    background: var(--border-strong);
    content: '';
  }

  .article-prose :global(.article-vertical-flow li) {
    position: relative;
    display: grid;
    grid-template-columns: minmax(10rem, 0.45fr) minmax(0, 1fr);
    gap: 1rem;
    margin: 0;
    padding: 0.78rem 1rem 0.78rem 2.5rem;
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    background: var(--surface-warm);
    color: var(--text-primary);
    line-height: 1.55;
  }

  .article-prose :global(.article-vertical-flow li::before) {
    position: absolute;
    top: 1.08rem;
    left: 0.68rem;
    width: 0.62rem;
    height: 0.62rem;
    border: 2px solid var(--surface-warm);
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 0 1px var(--accent);
    content: '';
  }

  .article-prose :global(.article-vertical-flow strong) {
    font-family: var(--font-mono);
    font-size: 0.84rem;
    overflow-wrap: anywhere;
  }

  .article-prose :global(.article-vertical-flow span) {
    color: var(--text-secondary);
    font-size: 0.8rem;
  }

  .article-prose :global(.article-vertical-flow--product strong) {
    font-family: var(--font-sans);
  }

  .article-prose :global(.article-rag-equation) {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem 0.9rem;
    align-items: center;
    justify-content: center;
    margin: 1.8rem 0 2rem;
    padding: 1.25rem 0;
    border-block: 1px solid var(--border-strong);
    color: var(--text-primary);
    font-family: var(--font-mono);
    font-size: clamp(1rem, 2.4vw, 1.28rem);
  }

  .article-prose :global(.article-rag-equation strong) {
    color: var(--accent-hover);
    font-size: 1.18em;
  }

  .article-prose :global(.article-rag-equation span[aria-hidden='true']) {
    color: var(--text-muted);
  }

  .article-prose :global(.article-bad-cases) {
    margin: 2rem 0 2.3rem;
    padding: clamp(1.1rem, 3vw, 1.5rem);
    border: 1px solid var(--border-strong);
    border-radius: var(--radius-md);
    background: var(--surface-soft);
  }

  .article-prose :global(.article-bad-cases header) {
    display: grid;
    gap: 0.25rem;
  }

  .article-prose :global(.article-bad-cases header p),
  .article-prose :global(.article-bad-cases header h4) {
    margin: 0;
  }

  .article-prose :global(.article-bad-cases header p) {
    color: var(--accent-hover);
    font-family: var(--font-mono);
    font-size: 0.74rem;
    font-weight: 680;
  }

  .article-prose :global(.article-bad-cases header h4) {
    font-size: clamp(1.12rem, 2vw, 1.32rem);
  }

  .article-prose :global(.article-bad-cases ol) {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
    margin: 1.2rem 0 0;
    padding: 0;
    list-style: none;
  }

  .article-prose :global(.article-bad-cases li) {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 0.75rem;
    padding-top: 0.9rem;
    border-top: 1px solid var(--border-strong);
  }

  .article-prose :global(.article-bad-cases li > span) {
    color: var(--text-muted);
    font-family: var(--font-mono);
    font-size: 0.74rem;
  }

  .article-prose :global(.article-bad-cases li strong) {
    color: var(--text-primary);
    font-family: var(--font-mono);
    font-size: 0.82rem;
  }

  .article-prose :global(.article-bad-cases li p) {
    margin: 0.35rem 0 0;
    color: var(--text-secondary);
    font-size: 0.84rem;
    line-height: 1.7;
  }

  .article-prose :global(.article-stage-summary) {
    margin: 2.2rem 0;
    padding: clamp(1.15rem, 3vw, 1.6rem);
    border: 1px solid color-mix(in srgb, var(--accent) 24%, var(--border-strong));
    border-radius: var(--radius-md);
    background: var(--accent-soft);
  }

  .article-prose :global(.article-stage-summary h3) {
    margin: 0;
    font-size: clamp(1.2rem, 2vw, 1.45rem);
  }

  .article-prose :global(.article-stage-summary ol) {
    margin: 1rem 0 0;
  }

  .article-prose :global(.article-stage-summary li) {
    color: var(--text-primary);
  }

  .article-prose :global(.article-problems) {
    margin: 2rem 0 2.4rem;
    padding: clamp(1.15rem, 3vw, 1.6rem);
    border: 1px solid color-mix(in srgb, var(--accent) 28%, var(--border-strong));
    border-radius: var(--radius-md);
    background: var(--accent-soft);
  }

  .article-prose :global(.article-problems__label) {
    margin: 0 0 0.9rem;
    color: var(--accent-hover);
    font-size: 0.78rem;
    font-weight: 700;
  }

  .article-prose :global(.article-problems ul) {
    margin: 0;
  }

  .article-prose :global(.article-problems li) {
    color: var(--text-primary);
  }

  .article-prose :global(hr) {
    margin: 3.25rem 0;
    border: 0;
    border-top: 1px solid var(--border-strong);
  }

  .article-prose :global(figure) {
    margin: 2.5rem 0;
  }

  .article-prose :global(img) {
    display: block;
    max-width: 100%;
    height: auto;
    border-radius: var(--radius-sm);
  }

  .article-prose :global(figcaption) {
    margin-top: 0.75rem;
    color: var(--text-muted);
    font-size: 0.76rem;
    line-height: 1.65;
    text-align: center;
  }

  .article-prose :global(.article-experience) {
    display: grid;
    gap: 0.72rem;
    margin-top: 4rem;
    padding-block: 1.4rem;
    border-block: 1px solid var(--border-strong);
  }

  .article-prose :global(.article-experience p) {
    margin: 0;
  }

  .article-prose :global(.article-experience__label) {
    color: var(--text-muted);
    font-size: 0.74rem;
    font-weight: 650;
  }

  .article-prose :global(.article-experience__title) {
    color: var(--text-primary);
    font-family: var(--font-display);
    font-size: clamp(1.18rem, 2vw, 1.42rem);
    font-weight: 650;
    line-height: 1.5;
  }

  .article-prose :global(.article-experience__link) {
    display: inline-flex;
    width: fit-content;
    margin-top: 0.2rem;
    color: var(--accent-hover);
    font-size: 0.86rem;
    font-weight: 650;
    text-decoration: underline;
    text-decoration-color: color-mix(in srgb, currentColor 32%, transparent);
    text-underline-offset: 0.2em;
  }

  .article-prose :global(table) {
    display: block;
    width: 100%;
    max-width: 100%;
    margin: 1.8rem 0 2.2rem;
    overflow-x: auto;
    border-top: 1px solid var(--border-strong);
    border-bottom: 1px solid var(--border-strong);
    font-size: 0.88rem;
    line-height: 1.65;
    overscroll-behavior-inline: contain;
  }

  .article-prose :global(th),
  .article-prose :global(td) {
    min-width: 8.5rem;
    padding: 0.8rem 0.85rem;
    vertical-align: top;
    white-space: normal;
  }

  .article-prose :global(th) {
    background: var(--surface-soft);
    color: var(--text-primary);
    font-weight: 650;
  }

  .article-prose :global(code) {
    overflow-wrap: anywhere;
  }

  .article-prose :global(pre) {
    max-width: 100%;
    margin: 1.5rem 0 2rem;
    padding: 1rem 1.1rem;
    overflow-x: auto;
    border: 1px solid var(--border-strong);
    border-radius: var(--radius-sm);
    background: var(--surface-soft);
    color: var(--text-primary);
    font-size: 0.86rem;
    line-height: 1.7;
    overscroll-behavior-inline: contain;
    tab-size: 2;
  }

  .article-prose :global(pre code) {
    overflow-wrap: normal;
    white-space: pre;
  }

  @media (max-width: 560px) {
    .article-prose {
      font-size: 1.0625rem;
    }

    .article-prose :global(h2) {
      margin-top: 3rem;
    }

    .article-prose :global(.article-flow) {
      gap-inline: 0.55rem;
      font-size: 0.88rem;
    }

    .article-prose :global(.article-problems) {
      padding-inline: 1rem;
    }

    .article-prose :global(.article-transform-list p),
    .article-prose :global(.article-capability-list > div),
    .article-prose :global(.article-vertical-flow li) {
      grid-template-columns: 1fr;
    }

    .article-prose :global(.article-transform-list p) {
      gap: 0.15rem;
      align-items: start;
    }

    .article-prose :global(.article-transform-list p > span[aria-hidden='true']) {
      rotate: 90deg;
      transform-origin: center;
    }

    .article-prose :global(.article-capability-list > div) {
      gap: 0.35rem;
    }

    .article-prose :global(.article-prompt-structure) {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .article-prose :global(.article-prompt-structure li:nth-child(-n + 3)) {
      border-top: 1px solid var(--border);
    }

    .article-prose :global(.article-prompt-structure li:nth-child(-n + 2)) {
      border-top: 0;
    }

    .article-prose :global(.article-prompt-structure li:not(:nth-child(3n + 1))) {
      border-left: 0;
    }

    .article-prose :global(.article-prompt-structure li:nth-child(even)) {
      border-left: 1px solid var(--border);
    }

    .article-prose :global(.article-vertical-flow li) {
      gap: 0.22rem;
    }

    .article-prose :global(.article-bad-cases ol) {
      grid-template-columns: 1fr;
    }
  }
</style>
