# Mizuki 重建进度

> 恢复工作时先阅读本文件，再运行 `git status` 与 `git log --oneline -8`。不要重新初始化仓库，不要重新拉取 Mizuki，也不要覆盖当前有效修改。

## 全站字体系统与排版节奏（2026-07-25）

| 项目         | 状态   | 当前真实情况                                                                                                     |
| ------------ | ------ | ---------------------------------------------------------------------------------------------------------------- |
| 正文无衬线   | 已完成 | 自托管 `Inter Variable` 与按 Unicode Range 拆分的 `Noto Sans SC Variable`；系统中文字体作为失败回退              |
| 展示字体     | 已完成 | `--font-display` 复用 Inter，仅用于首页 `TIANQI.Z` 与少量大型纯英文品牌标题，没有额外引入 Manrope                |
| 等宽字体边界 | 已完成 | `--font-mono` 只用于代码、项目编号、版本、slug、命令和极短技术参数；导航、状态、技术栈、普通英文标签均改用无衬线 |
| 主要字重     | 已完成 | 全站职责收敛为 400 / 500 / 600 / 700，清理 560、620、650、680、720、750、760、780、800、850 等非标准字重         |
| 中文阅读节奏 | 已完成 | 正文基线为 16px / 1.75，长文约 1.8；普通中文不增加字间距，正文强调统一为 600                                     |
| 字体加载     | 已完成 | 两套 Variable WOFF2 均由构建产物本地提供并使用 `font-display: swap`；未预加载全部中文字体分片，也未使用客户端 JS |

### 本轮工程与浏览器验证

- 依赖：`@fontsource-variable/inter@5.3.0` 与 `@fontsource-variable/noto-sans-sc@5.3.0`，字体许可证均为 OFL-1.1。
- 构建产物：共输出 105 个按字符范围拆分的 WOFF2，合计 4,847,472 bytes；首页浏览器实际命中 1 个 Inter 子集与 18 个 Noto Sans SC 子集，文件体积合计 1,046,152 bytes。HTML 未加入字体 preload。
- `npm run validate`：通过；Prettier、ESLint、Astro 检查和生产构建全部成功。Astro 检查 48 个文件，0 errors / 0 warnings / 0 hints；静态生成 17 个页面，Pagefind 索引 17 个页面。
- 浏览器尺寸：1440×900、1280×720、768×1024、430×932、390×844、360×800 均完成浅色与深色检查；无页面水平滚动、Header 内部溢出或 Hero / 项目标题横向溢出，首页保持唯一 H1。
- 字体职责：Header 品牌、中文导航、英文副标题、Hero 状态与岗位、项目状态/来源/技术栈均为无衬线；Hero `TIANQI.Z` 使用展示字体；项目编号与详情编号保留等宽字体。
- 长文：项目详情标题为 700，正文与列表为 400 / 1.8，`strong` 与引用强调为 600；代码与 `pre` 的全局等宽规则保留，当前抽查案例没有代码块或表格内容。
- 运行时：`document.fonts.status` 为 `loaded`；控制台没有字体 404、CORS、解码错误或新增运行时警告。生产输出仍为纯静态页面，未发现客户端 Node 内置模块或 Worker / server 产物。

## 深海动态 Hero 开屏（2026-07-25）

| 项目           | 状态   | 当前真实情况                                                                                                 |
| -------------- | ------ | ------------------------------------------------------------------------------------------------------------ |
| 深海背景资源   | 已完成 | 用户提供 1672×941 PNG；已正确转码为 171,756 bytes 的 `/images/banner/hero-bg.webp`，并登记资源来源与使用边界 |
| Hero 分层      | 已完成 | 背景图、深蓝遮罩、右上呼吸光、六个 CSS 气泡和独立 HTML 信息层；图片为装饰性空 Alt                            |
| 首次与返回动效 | 已完成 | 首次约 1.54 秒按品牌名、定位、描述、CTA、滚动提示依次入场；同一会话返回使用 280ms 快速淡入                   |
| 背景持续微动   | 已完成 | 16 秒 `scale(1 → 1.035)` 与 `translateY(0 → -7px)` 往返；不使用视频、Canvas 或第三方动画库                   |
| 滚动联动       | 已完成 | 900px 以上以 passive scroll、`requestAnimationFrame` 和 `--hero-progress` 驱动文字上移淡出与背景轻微放大     |
| 响应式裁切     | 已完成 | 桌面人物在左、文字在右；900px 以下改为底部文字布局，520px 以下 CTA 纵向排列并减少气泡                        |
| Reduced Motion | 已完成 | 静态显示全部内容，关闭入场、背景循环、气泡、呼吸光和滚动联动                                                 |
| 无 JavaScript  | 已完成 | `data-intro="pending"` 默认不隐藏内容；背景、姓名、岗位、价值主张和 CTA 均直接存在于静态 HTML                |

### 本轮工程与浏览器验证

- 图片：源文件扩展名为 `.webp` 的副本实际仍是 PNG 数据，因此使用项目已有 Sharp 从原始 PNG 重新转码；最终文件是真正的 WebP。
- `npm run check`：48 个文件，0 errors / 0 warnings / 0 hints。
- `npm run lint`：通过。
- `npm run build`：静态构建成功，生成 17 个页面；Pagefind 索引 17 页、1277 个词。
- 浏览器尺寸：1440×900、1280×720、1024×768、768×1024、430×932、390×844、360×800 均无水平溢出，Header 无内部溢出，Hero 与 CTA 完整可见。
- 窄屏：430、390、360px 下两个 CTA 均为 48px 高并纵向排列；背景裁切保留人物面部，首屏返回顶部按钮保持隐藏。
- 主题：1440×900 与 390×844 均检查浅色、深色 Header 和 Hero 对比度。
- 滚动联动：桌面滚动至 Hero 进度 `0.620` 时，文字透明度为 `0.5784`、上移约 26px，背景缩放约 `1.0217`；没有滚轮拦截或强制吸附。
- Reduced Motion：使用一次性强制分支验证 `data-intro="static"`、所有 Hero 动画名为 `none`；滚动 452px 后进度仍为 `0`，文字与背景无变换。验证后恢复正式媒体查询并重新构建。
- 会话与 Swup：新标签页验证 `full` 入场；进入在线简历再通过站内导航返回首页后验证 `quick` 入场。
- 交互：移动菜单打开、Escape 关闭与焦点恢复通过；搜索对话框打开后焦点进入输入框，Escape 关闭后焦点返回搜索按钮；返回顶部在首屏隐藏并可从页尾平滑返回 `scrollY = 0`。
- 控制台：两个验收标签页均为 0 条页面运行时错误。
- `dist/` 审计：17 个 HTML，站内链接缺失 0；首页 H1 数量 1；静态 HTML 直接包含背景、定位、描述和 CTA；客户端 `node:fs` / `node:path` 为 0，Worker / server 产物为 0。

## 首页沉浸式改版（2026-07-25）

| 项目          | 状态   | 当前真实情况                                                                            |
| ------------- | ------ | --------------------------------------------------------------------------------------- |
| 沉浸式 Hero   | 已完成 | 唯一 H1 为 `TIANQI.Z`；保留中文姓名、英文定位、中文岗位、价值主张、在线简历和项目 CTA   |
| 首页内容精简  | 已完成 | 首页固定展示 3 个真实项目，再进入 4 步产品方法和职业 CTA；未渲染不存在的 Latest Writing |
| 旧首页组件    | 已保留 | `MainGridLayout`、`ProfileCard`、`RightSidebar` 及其数据没有删除，只是不再挂载到首页    |
| 导航与移动端  | 已完成 | 移除重复简历按钮；搜索、主题与移动菜单保留 44px 触控目标；GitHub 进入移动菜单辅助区     |
| Hero 会话动效 | 已完成 | 首次约 1.6 秒分段入场；同会话返回快速淡入；存储失败与 Reduced Motion 使用静态状态       |
| Swup 生命周期 | 已完成 | 通过现有全局实例在 `page:view` 重新初始化 Hero，并用 AbortController 清理旧滚动监听     |
| 浮动控件      | 已完成 | 只保留返回顶部；首屏隐藏，滚动后显示，并使用系统安全区间距                              |
| 文档同步      | 已完成 | README 与 DESIGN 已记录新首页结构、动效边界和响应式策略                                 |

### 本轮工程与浏览器验证

- `npm ci`：通过；锁定依赖安装完成。npm 审计报告 22 个既有依赖漏洞（1 moderate、21 high），本轮没有执行可能引入破坏性升级的 `npm audit fix --force`。
- `npm run validate`：通过；全量 Prettier、ESLint、Astro 检查与生产构建成功。
- Astro 检查：48 个文件，0 errors / 0 warnings / 0 hints。
- 静态构建：17 个页面；Pagefind 索引 17 页、1270 个词。
- `dist/` 链接审计：17 个 HTML，站内链接缺失 0；首页 H1 数量 1；三个精选项目详情链接均存在。
- 静态边界：输出中没有 Worker / server 文件，也没有客户端 `node:fs` 或 `node:path`。
- 浏览器尺寸：1440×900、1280×720、1024×768、768×1024、430×932、390×844、360×800 均无水平溢出；900px 以下品牌名片隐藏。
- 主题：1440×900 与 390×844 的浅色、深色状态均完成视觉检查。
- Hero：新标签页验证 `full` 入场；Swup 进入在线简历和项目详情后返回首页验证 `quick` 状态。
- Reduced Motion：使用一次性本地强制媒体分支构建验证 `static`、无 Hero 动画、滚动进度保持 0、无缩放位移；测试后已恢复正式媒体查询。
- 交互：移动菜单、Escape、搜索焦点、Pagefind 查询、主题、返回顶部、在线简历、项目详情和 Swup 历史返回均通过。
- 无 JavaScript 基线：首页姓名、定位、CTA、三个项目、方法和职业入口均直接存在于静态 HTML；`data-intro="pending"` 不隐藏正文。

## 当前阶段（2026-07-17）

| 阶段               | 状态   | 当前真实情况                                                                                                                        |
| ------------------ | ------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| 0. 恢复检查        | 已完成 | 当前分支为 `main`；`backup-tech-theme` 存在；迁移前恢复提交 `634f4ef` 存在；旧站基线 `2e189c4` 保留                                 |
| 1. Mizuki 基础迁移 | 已完成 | 保留悬浮导航、Banner、三栏布局、左右侧栏、Swup、明暗主题与 Pagefind；包管理器为 npm，锁文件为 `package-lock.json`                   |
| 2. 首页重建        | 已完成 | 首页保持浅色、柔和、二次元博客感；本轮对 6 个首页结构文件做哈希核对，确认没有修改                                                   |
| 3. 项目索引与详情  | 已完成 | `/projects/` 与 7 个项目详情路由已完成；6 个公开 GitHub 项目依据 README 校对，另 1 个既有本地案例明确标注仓库未公开且不声明运行数据 |
| 4. About 内容页    | 已完成 | `/about/` 使用 Mizuki Markdown 内容集合，移除未经证实的年份、经历和数字，补充真实项目来源说明与 GitHub 入口                         |
| 5. Game 基础结构   | 已完成 | `/game/` 提供真实记录的字段模板、内容列表和无内容空状态；没有虚构游戏记录                                                           |
| 6. Stack 基础结构  | 已完成 | `/stack/` 只展示可从公开仓库、当前站点或本地案例确认的工具用途，并链接到对应证据项目                                                |
| 7. 工程验证        | 已完成 | `npm install`、`npm run lint`、`npm run check`、`npm run build` 已通过；Astro 与 Pagefind 生产构建成功                              |
| 8. 其余内页定稿    | 未完成 | `/resume/`、`/moments/`、`/archive/`、`/credits/` 与 `/404.html` 尚未进入下一轮内容和交互验收                                       |
| 9. 正式资源与部署  | 未完成 | 等待真实截图、简历、邮箱与正式域名；设置 `SITE_URL` 后再完成正式部署配置                                                            |

## 第二阶段完成内容

- `/projects/` 增加来源统计，明确区分 6 个已核验公开仓库和 1 个仓库未公开的本地案例。
- 7 个项目详情页统一展示内容来源、真实项目名、视觉代号、职责、工作流、AI 设计、人工节点、评测关注项、失败边界、迭代记录与下一步。
- 删除项目 frontmatter 中的封面与截图占位路径；本轮没有生成、下载或热链图片。
- 公开项目内容依据对应 GitHub README 校对：
  - `ai-product-evaluation-workbench`
  - `mobile-uiagent-evaluation-lab`
  - `automate-ai-car-copilot`
  - `ai-game-content-copilot`
  - `ai-pdf-learning-assistant`
  - `elysia-ai-character-agent`
- `ai-game-growth-workflow` 没有可核验的公开 GitHub 仓库，因此不添加虚构链接，不填写投放、转化、效率或其他运行结果。
- `/about/` 改为可核验的个人方法、项目实践、能力边界与求职方向说明。
- `/game/` 保留空状态，并建立以后录入真实体验记录所需的字段与展示结构。
- `/stack/` 建立工具、用途、场景、证据项目与下一步之间的对应关系。
- 首页结构文件的 Git blob 哈希与阶段开始前记录完全一致。

## 验证结果

- `npm install`：通过；依赖已是最新状态，审计为 0 个漏洞。
- `npm run lint`：通过。
- `npm run check`：通过；47 个文件，0 errors / 0 warnings / 0 hints。
- `npm run build`：通过；Astro 生成 17 个页面。
- Pagefind：通过；索引 17 个页面、1239 个词。
- `dist/`：已生成；包含 `/projects/`、7 个项目详情、`/about/`、`/game/`、`/stack/` 与 Pagefind 索引。

## 下一轮内页范围

本轮按要求停止，不继续制作以下内页：

- `/resume/`：接入真实简历 PDF 后完成下载入口与内容校对
- `/moments/`：仅在有真实文章后补充内容和归档体验
- `/archive/`：结合真实文章与项目记录完成归档逻辑
- `/credits/`：校对依赖、主题与资源署名
- `/404.html`：补充最终文案和可选插画后的视觉验收

还可在下一轮对本阶段页面进行移动端和键盘交互验收，但不要改回虚构数据或不存在的项目能力。

## 等待用户补充

- 6 个公开 GitHub 项目的真实界面截图；本地案例若需要继续展示，也需提供可公开的工作流截图或文档
- 首页 Banner：`home.webp`，可选 `home-1440.webp`、`home-960.webp` 与 `character-mobile.webp`
- 个人资料头像：`avatar.webp`
- 可选看板娘立绘：`character.webp`
- 可选 About 与 404 插画
- 3 份正式简历 PDF
- 真实邮箱、正式域名和需要公开的其他真实外链

完整资源路径与降级规则见 `ASSET_CHECKLIST.md`。

## 可恢复节点

- 旧站提交：`2e189c4`
- 旧站备份分支：`backup-before-rebuild`
- 科技主题备份分支：`backup-tech-theme`
- Mizuki 迁移前检查点：`634f4ef`
- 首页重建提交：`a6fe16c`
- 当前开发分支：`main`

## 完整改版（2026-07-31）

- 主导航收敛为首页、项目、研究、关于、简历；搜索、主题和 GitHub 保留为全局工具。
- 首页改为浅色招聘阅读路径，包含 4 个精选项目、4 项能力、7 步工作流、Research、关于摘要和求职 CTA。
- 深海图缩为右侧品牌插图并明确标注“非项目运行截图”；删除全屏等待、气泡粒子、滚动视差和 Hero 会话状态。
- 7 个项目新增 `filters` 与可核验 `outcome`；项目索引支持 URL 筛选、计数、前进后退和无结果状态。
- 项目详情统一为问题、方案、职责、产出、AI 必要性、人工节点、产品流程、原型证据、评测、失败边界、迭代、反思、来源和下一个项目。
- 新增 Research 内容集合、列表、文章详情与首篇产品方法文章；草稿自动排除路由、首页、搜索和 RSS。
- `/game/`、`/moments/`、`/agent/`、`/auto/` 已更新重定向。
- 增加 Person、WebSite、CreativeWork、Article 结构化数据，并按 `SITE_URL` 条件生成 canonical、sitemap、robots 与 RSS。
- 保留 Cloudflare 配置，增加 Netlify `npm run build` / `dist` 与 Node 22 配置。
- 删除 11 个无引用组件、旧 `character.css`、重复 `HeroOpening.astro` 与两个约 2.18MB 的重复深海资源。
- 设计规范改为冷中性浅色、8–16px 圆角、约 70ch 正文与开放排版；深色主题继续支持。

### 最终验证

- `npm run validate`：通过；Prettier、ESLint、Astro check、静态构建与 Pagefind 全部完成。
- Astro check：38 个文件，0 errors / 0 warnings / 0 hints。
- Astro build：生成 17 个页面；Pagefind 索引 17 个页面、1276 个词。
- 无 `SITE_URL` 与临时 `SITE_URL=https://portfolio.example` 两种构建均通过；已核对 canonical、sitemap、robots、RSS 与结构化数据的条件输出。
- 生成站点链接审计：21 份 HTML，0 个空 `href/src`，0 个缺失内部目标。
- 浏览器响应式审计：1440、1280、1024、768、430、390、360px 下检查首页、项目列表、项目详情、研究文章、About、Resume 与 404，无横向溢出、标题裁切或损坏图片。
- 浏览器交互审计：项目筛选与历史返回、研究分类空状态、移动菜单与 Escape、搜索焦点与 Escape、浅色/深色主题、Swup 历史返回、旧路由重定向均通过；控制台无 error / warning。
- 额外复验：1280×720 首屏 CTA 保持可见；430px Research 目录只在自身横向滚动，不再撑宽页面。

## 暖色编辑式视觉校正（2026-07-31）

- 全站由冷色科技感校正为“暖色个人博客 × 清晰作品集 × 克制游戏 AI 个性”：页面 `#f7f4ef`、白色内容面、深墨文字、橙色重点，紫色只作兼容语义色。
- 标题改用系统宋体栈，正文改用苹方、微软雅黑与本地 Noto Sans SC；展示标题上限收敛到约 68px。
- Header 增加“随想”入口并保持首页、项目、研究、关于、简历、搜索、主题与 GitHub；移动菜单改为 Header 下方完整视口高度，打开时锁定页面滚动。
- 首页 Hero 收敛为姓名、岗位、求职状态、短价值说明和三个真实行动；深海图只作为右侧局部品牌插图。
- 首页固定展示 4 个精选项目；新增精简 `ProjectCard`、`ArticleCard` 与 `InfoCard`，移除列表卡中的状态、职责、结果与装饰编号过载。
- 研究卡、项目详情、Research、About、Resume、404、Archive、Stack 与 Credits 已统一到暖色、开放、低阴影的视觉语言。
- 全站入场统一为一次性 Intersection Observer：28px 位移、约 700ms、组内 90ms 递增；Reduced Motion 与无 JavaScript 时内容直接可见。
- 浏览器验收发现并修复：首页横向研究卡的封面宽高比挤压文本、移动菜单因 Header 模糊背景只剩 124px 高、极窄屏 1px 溢出、长文章标题六行换行，以及无 404 素材时的大面积空装饰占位。
- `npm run validate`：Prettier、ESLint、Astro check、静态构建与 Pagefind 全部通过；40 个 Astro 文件 0 errors / 0 warnings / 0 hints，生成 17 个页面并索引 1242 个词。
- 浏览器检查：1280×720、约 547px 与 319px 边界宽度下，首页、项目列表、项目详情、研究列表、文章、About、Resume 和 404 无内容横向溢出；项目 URL 筛选、历史返回、移动菜单和主题切换通过，控制台无页面运行时错误。
- 无 `SITE_URL` 与临时 `SITE_URL=https://portfolio.example` 两种构建均通过；canonical、sitemap、robots、RSS、Person、WebSite、CreativeWork 与 Article 结构化数据均已核对。
- 最终产物审计：21 份 HTML，0 个空 `href/src`，0 个缺失内部目标；RSS 只含已发布 Research，不含项目页。临时测试域名已从最终 `dist` 恢复移除。
