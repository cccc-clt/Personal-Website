# Design System

## 设计场景

招聘者在白天办公室使用小尺寸笔记本快速阅读一份 AI 产品候选人档案。默认浅色，信息密度适中，视觉强调来自宋体标题、暖色留白、真实项目封面和少量橙色，而不是沉浸式暗场、游戏 UI 或动画等待。

品牌关键词：温暖、清晰、可核验、开放、带克制的游戏 AI 个性。

## 色彩

实现入口为 `src/styles/tokens.css`。

### 浅色主题

- 页面：`#f7f4ef`
- 内容面：`#ffffff`
- 柔和内容面：`#f3eee7`
- 主文字：`#211e1b`
- 次文字：`#6f6861`
- 辅助文字：`#8a8179`
- 主强调：`#c98232`
- 强调文字：`#8f531f`
- 柔和强调面：`#f6eadb`

### 深色主题

- 页面：`#1f1b18`
- 内容面：`#2a2521`
- 主文字：`#f4eee7`
- 次文字：`#c8beb4`
- 强调：`#dfa35e`

### 规则

- 正文与背景保持 WCAG AA 对比度。
- 不使用渐变文字。
- 紫色仅作为少量语义或图像兼容色，不形成蓝紫 AI 科技主视觉。
- 同一组件不同时使用 1px 边框和宽软阴影作为装饰。

## 字体

- 展示标题：`Songti SC` / `STSong` / 系统宋体
- 中文正文与界面：`PingFang SC` / `Microsoft YaHei` / `Noto Sans SC Variable`
- 拉丁和降级：系统无衬线字体
- 代码与数字：`Cascadia Mono` / 系统等宽字体

字号以流体标题和 0.72–1.2rem 的正文层级为主。首页与内页展示标题最大约 4.25rem，避免在小笔记本上形成 6 行以上的标题墙。正文行宽控制在 65–70ch，行高约 1.75–1.85。

## 布局

- 页面最大宽度：1280px
- 桌面外边距：32px
- 移动外边距：20px
- 主要区段间距：约 4.5–7.5rem，按叙事节奏变化
- 案例和文章正文：侧边目录 + 最大 70ch 内容列
- 断点以内容为准，内容门户在 1100px 以下收起侧栏，卡片主要使用 960px、700px 和 520px

首页精选项目与研究内容均使用桌面一行三张的文章式卡片；四项能力使用轻量 `InfoCard`，七步工作流只在 About 展开。项目列表保留内容门户；随笔与研究聚合页使用开放页头、两组轻量筛选与两列内容卡片，移动端转为单列。内页 Hero 使用开放单列排版，不使用通用渐变圆形装饰。

## 圆角与深度

- 小控件：8px
- 按钮与输入：8–12px
- 图片、卡片、Dialog、内容面：12–18px
- 胶囊仅用于标签
- 阴影只表达悬浮或 Dialog 层级；普通内容面依靠背景、边界和留白

品牌插图可以使用一个明显的非对称大圆角作为图像裁切，不扩散到所有卡片。

## 组件

### Header

固定顶部，主导航为首页、项目、随笔与研究、关于、简历。滚动后出现轻边界和短阴影。移动菜单显式占满 Header 以下的视口，打开时锁定页面滚动，并支持 Escape、焦点恢复与 Swup 生命周期。

### Button

主按钮使用深墨色或橙棕强调；次按钮使用白色内容面和边框；文本行动使用箭头链接。所有按钮有 hover、active 和 focus-visible 状态，不允许空 `href`。

### Project Card

真实产品封面、截图说明、项目类型、标题、摘要、最多三个标签和阅读入口组成一个可点击案例单元。详细状态、职责和真实产出留在项目详情，避免列表卡过载。项目索引两列，移动端单列。

### Article Card

上方封面、内容类型、日期、宋体标题、三行摘要、最多四个标签、页数与阅读时间组成文章卡。报告优先使用 PDF 真实首页，并同时提供“阅读摘要”和“查看 PDF”；缺少真实研究封面时可复用品牌插图，但必须标注“非研究证据”。

### Info Card

只承载能力名称、短标题和一段说明。使用暖白内容面、细边界和轻微悬浮，不增加装饰编号、图标或多层嵌套。

### Filter

项目使用 `?filter=`、`?source=` 与 `?status=`，随笔与研究使用 `?type=` 与 `?category=`。按钮包含真实计数、`aria-pressed` 与无结果状态，并支持浏览器前进后退。搜索复用全局 Pagefind Dialog。

### Article

自动目录、阅读时间、更新时间、移动端可横向滚动表格、代码复制、提示块、来源说明、相关阅读和返回顶部。

### Dialog

图片放大使用原生 `dialog`，遮罩点击和 Escape 可关闭，关闭后焦点回到触发按钮。

## 图片规则

- `public/images/banner/hero-bg.webp` 是首页局部品牌插图，明确标注不属于项目证据。
- 7 张项目封面均为本地真实产品截图，详情首屏和卡片附近必须准确标注。
- `gallery` 只接收真实截图或过程证据；缺失时显示说明，不伪造运行界面。
- 图片必须提供真实替代文本、固定宽高并按首屏优先级加载。

## 动效

- 内容默认可见。
- 保留一次性 28px 淡入、图片 1.025 倍悬停、箭头和按钮反馈。
- 同组内容使用 90ms 递增延迟，元素进入后立即取消观察，不重复播放。
- 不使用全屏开场等待、气泡粒子、滚动视差、弹跳缓动或循环吸睛动画。
- `prefers-reduced-motion` 下取消位移和非必要过渡。

## 禁止模式

- 侧边彩色粗边条
- 渐变文字
- 32px 以上通用卡片圆角
- 1px 边框叠加宽软阴影的幽灵卡片
- 每节重复的全大写眉题或 `01 / 02 / 03` 装饰编号
- 三列完全相同的功能卡
- 蓝紫“AI 科技”渐变背景或游戏 HUD 装饰
- 概念图冒充真实运行截图
- 无来源数据、虚假外链或死按钮

## 验证尺寸

首页、项目列表、项目详情、Research、About、Resume 与 404 至少检查：

`1440 / 1280 / 1024 / 768 / 430 / 390 / 360`

同时检查浅色、深色、Reduced Motion、键盘导航、无 JavaScript 基线、内部链接和控制台错误。
