# Design System

## 设计场景

招聘者在白天办公室使用小尺寸笔记本快速阅读一份 AI 产品候选人档案。默认浅色，信息密度适中，视觉强调来自文字层级、真实项目封面和少量钢蓝色，而不是沉浸式暗场或动画等待。

品牌关键词：冷静、可核验、开放、带幻想余韵。

## 色彩

实现入口为 `src/styles/tokens.css`。

### 浅色主题

- 页面：`#f2f5f7`
- 内容面：`#ffffff`
- 柔和内容面：`#e9eef2`
- 主文字：`#17202d`
- 次文字：`#475667`
- 辅助文字：`#697888`
- 主强调：`#4d7895`
- 强调文字：`#285a7a`
- 柔和强调面：`#dce9ef`

### 深色主题

- 页面：`#141a21`
- 内容面：`#1e2630`
- 主文字：`#eff4f6`
- 次文字：`#b9c4cc`
- 强调：`#8db3c7`

### 规则

- 正文与背景保持 WCAG AA 对比度。
- 不使用渐变文字。
- 粉、紫、绿色仅作为少量语义或图像兼容色，不形成蓝紫 AI 渐变主视觉。
- 同一组件不同时使用 1px 边框和宽软阴影作为装饰。

## 字体

- 中文与主要界面：`Noto Sans SC Variable`
- 拉丁和降级：`Inter Variable`
- 代码与数字：`Cascadia Mono` / 系统等宽字体

字号以流体标题和 0.72–1.2rem 的正文层级为主。展示标题最大约 6rem，字距不得小于 `-0.04em`。正文行宽控制在 65–70ch，行高约 1.75–1.85。

## 布局

- 页面最大宽度：1320px
- 桌面外边距：32px
- 移动外边距：16px
- 主要区段间距：约 6–11rem，按叙事节奏变化
- 案例和文章正文：侧边目录 + 最大 70ch 内容列
- 断点以内容为准，主要使用 920px、820px、720px、680px 和 520px

首页使用不对称项目网格；能力和工作流采用线性分隔，不做四张相同功能卡。内页 Hero 使用上下文列 + 标题列，不使用通用渐变圆形装饰。

## 圆角与深度

- 小控件：8px
- 按钮与输入：8–12px
- 图片、卡片、Dialog、内容面：12–16px
- 胶囊仅用于标签
- 阴影只表达悬浮或 Dialog 层级；普通内容面依靠背景、边界和留白

品牌插图可以使用一个明显的非对称大圆角作为图像裁切，不扩散到所有卡片。

## 组件

### Header

固定顶部，主导航为首页、项目、研究、关于、简历。滚动后出现轻边界和短阴影。移动菜单覆盖页面，并支持 Escape、焦点恢复与 Swup 生命周期。

### Button

主按钮使用实色钢蓝或深墨色；次按钮使用白色内容面和边框；文本行动使用箭头链接。所有按钮有 hover、active 和 focus-visible 状态，不允许空 `href`。

### Project Card

封面、概念图说明、状态、用户问题、职责、真实产出和阅读入口组成一个可点击案例单元。项目索引两列，移动端单列。概念图必须出现“非运行截图”说明。

### Filter

项目使用 `?filter=`，Research 使用 `?category=`。按钮包含计数、`aria-pressed` 与无结果状态，并支持浏览器前进后退。

### Article

自动目录、阅读时间、更新时间、移动端可横向滚动表格、代码复制、提示块、来源说明、相关阅读和返回顶部。

### Dialog

图片放大使用原生 `dialog`，遮罩点击和 Escape 可关闭，关闭后焦点回到触发按钮。

## 图片规则

- `public/images/banner/hero-bg.webp` 是首页局部品牌插图，明确标注不属于项目证据。
- 7 张项目封面是概念图，详情首屏和卡片附近必须标注。
- `gallery` 只接收真实截图或过程证据；缺失时显示说明，不伪造运行界面。
- 图片必须提供真实替代文本、固定宽高并按首屏优先级加载。

## 动效

- 内容默认可见。
- 保留一次性轻入场、图片微漂移、箭头和按钮悬停。
- 不使用全屏开场等待、气泡粒子、滚动视差、弹跳缓动或循环吸睛动画。
- `prefers-reduced-motion` 下取消位移和非必要过渡。

## 禁止模式

- 侧边彩色粗边条
- 渐变文字
- 32px 以上通用卡片圆角
- 1px 边框叠加宽软阴影的幽灵卡片
- 每节重复的全大写眉题或 `01 / 02 / 03` 装饰编号
- 三列完全相同的功能卡
- 蓝紫“AI 科技”渐变背景
- 概念图冒充真实运行截图
- 无来源数据、虚假外链或死按钮

## 验证尺寸

首页、项目列表、项目详情、Research、About、Resume 与 404 至少检查：

`1440 / 1280 / 1024 / 768 / 430 / 390 / 360`

同时检查浅色、深色、Reduced Motion、键盘导航、无 JavaScript 基线、内部链接和控制台错误。
